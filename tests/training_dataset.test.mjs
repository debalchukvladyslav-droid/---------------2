import test from'node:test';import assert from'node:assert/strict';import{isHumanGold,toTrainingRecord,splitChronologically}from'../lib/training_dataset.js';
test('dataset excludes pending and auto reviewed labels',()=>{assert.equal(isHumanGold({review_status:'pending'}),false);assert.equal(isHumanGold({review_status:'approved',reviewed_by:'u',reviewed_pattern_key:'x',review_note:'[auto] ok'}),false)});
test('dataset emits chat shape without identities',()=>{const r=toTrainingRecord({trade_date:'2026-01-01',review_status:'corrected',reviewed_by:'u',reviewed_pattern_key:'early_orb',review_note:'Wait',source_snapshot:{ticker:'XYZ'},outcome:{pnl:-20}});assert.equal(r.messages.length,3);assert.doesNotMatch(JSON.stringify(r),/reviewed_by/)});
test('dataset split is chronological',()=>{const rows=['03','01','02','04','05','06','07','08','09','10'].map(d=>({messages:[],metadata:{trade_date:`2026-01-${d}`}}));const r=splitChronologically(rows);assert.equal(r[0].metadata.trade_date,'2026-01-01');assert.equal(r.at(-1).metadata.split,'test')});
