import { supabase } from './supabase.js';
import { showToast } from './utils.js';
async function downloadExcel(button) { const original=button.textContent; button.disabled=true; button.textContent='Формую XLSX…'; try { const {data}=await supabase.auth.getSession(); const token=data?.session?.access_token; if(!token) throw new Error('Потрібна активна сесія STRUM.'); const response=await fetch('/api/export',{headers:{Authorization:`Bearer ${token}`}}); if(!response.ok){const payload=await response.json().catch(()=>({}));throw new Error(payload.error||`Export HTTP ${response.status}`);} const blob=await response.blob(); const filename=(response.headers.get('content-disposition')||'').match(/filename="?([^";]+)"?/i)?.[1]||'STRUM-trading-journal.xlsx'; const url=URL.createObjectURL(blob); const anchor=document.createElement('a'); anchor.href=url; anchor.download=filename; document.body.appendChild(anchor); anchor.click(); anchor.remove(); URL.revokeObjectURL(url); showToast('Excel export готовий'); } catch(error){showToast(`Не вдалося створити XLSX: ${error.message}`);} finally{button.disabled=false;button.textContent=original;} }
export function initExcelExport(){document.querySelectorAll('[data-action="export-xlsx"]').forEach((button)=>{if(button.dataset.bound)return;button.dataset.bound='true';button.addEventListener('click',()=>downloadExcel(button));});}
